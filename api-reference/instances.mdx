---
title: "Instances Function"
description: "Functions that interact to instance."
icon: "code"
---

### Get Instances

```lua
getinstances() : {Instances} <table>
```

<Info>
  getinstances, get_instances
</Info>

Returns a table containing all instances in the game.

### Get Nil Instances

```lua
getnilinstances() : {NilInstances} <table>
```

<Info>
  getnilinstances, get_nil_instances, getnils
</Info>

Returns a table containing instances with the `Parent` property set to nil.

### Get Scripts

```lua
getscripts() : {LuaSourceContainer} <table>
```

<Info>
  getscripts, get_scripts
</Info>

Returns a table containing all scripts in the game.

### Get Modules

```lua
getmodules() : {ModuleScript} <table>
```

<Info>
  getmodules, get_modules
</Info>

Returns a table containing all module scripts in the game.

### Get Loaded Modules

```lua
getloadedmodules() : {LoadedModules} <table>
```

<Info>
  getloadedmodules, get_loaded_modules
</Info>

Returns a table containing loaded modules.

### Get Running Scripts

```lua
getrunningscripts() : {BaseScript} <table>
```

<Info>
  getrunningscripts, get_running_scripts
</Info>

Returns a table containing running scripts.

### Lock Instance

```lua
lockinstance(target: Instance) : () <void>
```

<Info>
  lockinstance
</Info>

Locks the `instance.Parent` property, preventing it from being changed.

### Get Locked Instances

```lua
getlockedinstances() : {LockedInstances} \<table\>
```

<Info>
  getlockedinstances
</Info>

Returns a table containing locked instances.

### Restrict Instance

```lua
restrictinstance(target: Instance) : () <void>
```

<Info>
  restrictinstance, rinstance
</Info>

Prevents the instance from getting child or descendant added.

### Free Instance

```lua
freeinstance(target: Instance) : () <void>
```

<Info>
  freeinstance
</Info>

Un-restrict the restricted instance.

### Get Restricted Instances

```lua
getrestrictedinstances() : {RestrictedInstances} <table>
```

<Info>
  getrestrictedinstances, getrestricts, getrestricted
</Info>

Returns the table containing restricted instances.

### Get Properties

```lua
getproperties() : {string} <table>
```

<Info>
  getproperties
</Info>

Returns the table containing the properties of the instance.

### Get Hui

```lua
gethui() : CoreGui <instance>
```

<Info>
  gethui, get_hidden_ui, gethiddenui, get_hui
</Info>

Returns a CoreGui.

### Is Scriptable

```lua
isscriptable(obj: Instance, prop: string) : (scriptable) <boolean>
```

<Info>
  isscriptable, is_scriptable
</Info>

Checks the property of the object and then returns a boolean wether it's scriptable or not.

### Is Network Owner

```lua
isnetworkowner(part: BasePart) : (UserNetworkOwner) <boolean>
```

<Info>
  isnetworkowner, is_network_owner
</Info>

Checks the `part` and then returns a boolean wether it's networkship is set to the local player (user) or not.